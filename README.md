Codes related to BigQuery

# tfvars
- Rename the file `terraform.tfvars.template` to `terraform.tfvars`.
- Define values for variables that are blank.

# Commands
- `terraform init`
- `terraform plan`
- `terraform apply`
